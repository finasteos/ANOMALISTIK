node_modules/
build/
dist/
coverage/
.DS_Store
*.log
.env*
!.env.example

# graft's local graph cache — regenerable, not committed (run `graft build`).
/graft/

# RNG commitment journal + hash-chained ledger — runtime state (D4).
data/rng_sessions/.commitments.json
data/rng_sessions/sessions.jsonl
