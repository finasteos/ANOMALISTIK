node_modules/
build/
dist/
coverage/
.DS_Store
*.log
.env*
!.env.example

# graft's local graph cache — regenerable, not committed (run `graft build`).
/graft/

# RNG commitment journal — runtime state, regenerated on session start (D4).
data/rng_sessions/.commitments.json
